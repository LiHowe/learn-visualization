type LinearGradientOption = {
    p0: [number, number],
    size: number
}

type RadialGradientOption = {
    p0: [number, number, number],
    size: number
}

// 线性渐变
export class LinearGradient {
    constructor(opt: LinearGradientOption, ctx: CanvasRenderingContext2D) {
        
    }
}

// 径向渐变
export class RadialGradient {
    constructor(opt: RadialGradientOption, ctx: CanvasRenderingContext2D) {

    }
}